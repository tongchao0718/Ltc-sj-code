---
description: 子应用文档四段式目录结构（新建/补文档时必须遵循）
globs: AIEP-WEB/src/docs/子应用文档/**
alwaysApply: false
---

# 子应用文档目录规范

新建或补全子应用文档时，**禁止**在 `<app-code>/` 根目录平铺文档；必须按 `sample-app` 四段式目录创建。

## 根路径

`AIEP-WEB/src/docs/子应用文档/<app-code>/`（`<app-code>` 与 `gate-config.app_code`、SDD `app_code` 一致）

## 标准目录（四段，缺一不可）

```text
<app-code>/
├── 01-需求与设计/       # 需求、PRD、SDD、映射表
├── 02-研发与测试/       # 详细设计、库表、验收 GWT、G3 任务
├── 03-发布与复盘/       # 应用操作说明书
└── 04-AI治理与审计/    # gate-config、SDD 校验、门禁检查、gate-report
```

## 文档落位（从模板复制后改名）

| 目录 | 典型文件 |
|------|----------|
| `01-需求与设计/` | `01-需求说明书.md`、`03-PRD设计评审文档.md`、`04-界面设计文档.md`、`SDD-v1.0.md` + `.json`、`页面-路由-接口-数据表映射表.md`、`17-需求确认单.md`、`19-G2-A界面确认记录.md`、`20-G2-B技术补齐评审记录.md` |
| `02-研发与测试/` | `详细设计文档.md`、`数据库设计文档.md`、`验收脚本-GWT.md`、`14-G3开发任务拆解表.md` |
| `03-发布与复盘/` | `应用操作说明书.md`、`发布记录单.md` |
| `04-AI治理与审计/` | `gate-config.json`、`SDD校验清单.md`、`G2-G3关卡自动门禁检查.md`、`gate-report.json` |

模板来源：`核心文档/AI+产品落地/02-子应用通用模板/`（复制后去「模板」后缀、替换 `<app-code>` 等占位符）。

## 执行要求

1. 先 `mkdir` 四个阶段目录，再写入文档；不得跳过目录直接在 `<app-code>/` 下建文件。
2. `gate-config.json` 内 `paths` 须与上述目录一致（见 `sample-app/04-AI治理与审计/gate-config.json`）。
3. 完整文件清单与关卡最小产物见 `核心文档/AI+产品落地/03-其他过程文档-参考/调整方案.md` §七、`README-子应用文档模板使用说明.md` §0。

## 反例

- `docs/子应用文档/my-app/需求说明书.md`（根目录平铺，已废止）
- 仅建 `01-需求与设计/` 而缺 `04-AI治理与审计/`（门禁脚本无法定位配置）
