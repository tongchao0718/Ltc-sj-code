# AI 助手说明（Cursor / Trae 等）

- **事实来源**：本仓库的 **`src/`**、**`vite.config.js`**、**`package-lock.json`**，不是《创建指南》全文。
- **版本基线**：根目录 **`framework-baseline.json`**；改动依赖后应同步更新该文件与 **`文档对齐清单.md`**（若适用）。
- **路由约定**：应用中心 **`/app-center`**；帮助 / 关于为页面 **`/help`**、**`/about`**；审查 **`/review-center`**；示例子应用 **`/sample-app/*`**（与 `src/router/index.js` 一致）。
- **禁止**：在未读现有代码的情况下，按旧文档从零生成第二套目录结构或把主应用打成 `dist/main` + IIFE（与现网不符）。
- **人类可读摘要**：**`快速初始化.md`**。
