/**
 * sample-app PRD 标注内容（Workflow A 初始化）
 * 对应：需求说明书 SA-1~SA-5、PRD 模块 M01~M05
 */

export const SAMPLE_APP_ANNOTATIONS = {
  1: {
    featureId: 'SA-1',
    featureName: '工作台指标与图表',
    moduleName: 'M01 仪表盘工作台',
    pageRoute: '/sample-app/dashboard/workplace',
    markdown: `## 需求来源

**需求编号：SA-1** | **模块：M01** | **页面：WorkplacePage** | **路由：** \`/sample-app/dashboard/workplace\`

### 显示样式

- 页面可访问，无白屏；欢迎语展示当前用户名称。
- **统计卡片**：4 项核心指标（内容量、活跃、评论、增长率），含单位与涨跌标识。
- **内容数据**：折线/面积图区域高度约 289px，标题「内容数据」+「查看更多」。
- **线上热门内容**：Tab 切换（文本/图文/视频），表格含排名、标题、点击量、日涨幅。
- **内容类型占比**：饼图区域。
- 右侧：**快捷操作**网格、**最近访问**列表。

### 交互与排序

1. 页面加载时调用 \`GET /api/dashboard/data\`（入参 \`dateRange\` 可选）。
2. 接口返回 \`code/message/data/requestId\`；失败时展示可读错误，保留空态占位。
3. 热门内容 Tab 切换仅切换表格数据源，不整页刷新。
4. 空态：无数据时卡片与图表区域显示空态文案，不可留空白块。

### 业务定义

- 数据来自 \`dashboard_metric_snapshot\`（见映射表）。
- 指标口径与 Arco Pro 工作台演示一致，用于验证子应用嵌入与图表组件（EChart）。

### 备注

> 验收（AT-01）：Given 用户进入工作台，When 页面加载完成，Then 核心指标卡片与图表渲染成功；空态可见。

**非功能（NFR-1）**：1366×768 与 1920×1080 布局可用，图表区域可横向滚动或自适应缩放。`
  },
  2: {
    featureId: 'SA-2',
    featureName: '列表筛选与查询',
    moduleName: 'M02 列表查询',
    pageRoute: '/sample-app/list/search-table',
    sections: {
      functional:
        '筛选区字段：集合编号、集合名称、内容体裁、筛选方式、创建时间（区间）、状态（全部/已上线/已下线）。操作按钮：查询（主按钮）、重置（次按钮）。筛选区与表格区以分割线区分。',
      interaction:
        '1. 查询：根据当前筛选条件请求 GET /api/list/query，入参 keyword, status, pageNo, pageSize 等。\n2. 重置：清空所有筛选字段并触发查询。\n3. 筛选条件变更后分页重置为第一页（产品设计文档 §3.1）。\n4. 请求失败：展示错误提示，不静默失败（NFR-3）。',
      dataModel:
        '表：sample_list_item。状态枚举 online / offline。接口契约见 SDD §列表查询；标准编码见 03-PRD 第七章。',
      other: '验收 AT-02：Given 用户设置筛选条件，When 执行查询并翻页，Then 结果按条件返回且分页重置规则正确。'
    }
  },
  3: {
    moduleName: '列表表格与行操作（M02 / SA-2）',
    markdown: `## 需求来源

**需求编号：SA-2**（续） | **锚点：表格区域**

### 显示样式

- 工具栏：**新建**、**批量导入**、**下载**；右侧刷新/密度/列设置（演示态可占位）。
- 表格列：序号、集合编号、集合名称、内容体裁、筛选方式、内容量、创建时间、状态、**操作**。
- 状态以标签展示（绿色已上线 / 灰色已下线）。
- 底部分页：总条数、上一页/下一页、当前页码。

### 交互与排序

1. 行内 **查看**：进入详情或抽屉（演示为按钮占位）。
2. 分页切换时保持当前筛选条件，仅变更 \`pageNo\`。
3. 无数据时表格区域展示空态。

### 业务定义

- 内容体裁：图文 / 横版短视频 / 竖版小视频。
- 筛选方式：人工筛选 / 规则筛选。

### 备注

> 行内操作与工具栏需求与筛选区分开展示，但同属 SA-2 列表查询能力。`
  },
  4: {
    moduleName: '分步表单提交（M03 / SA-3）',
    markdown: `## 需求来源

**需求编号：SA-3** | **模块：M03** | **页面：StepFormPage** | **路由：** \`/sample-app/form/step\`

### 显示样式

- 三步步骤条：选择基本信息 → 输入渠道信息 → 完成创建。
- 步骤 1：活动名称、渠道类型、推广时间等字段。
- 步骤 2：推广地址（URL）、广告来源、媒介、关键词、推送提醒、广告内容（多行）。
- 步骤 3：提交成功摘要。

### 交互与排序

1. 链路：**校验 → 提交中 → 成功回写 → 失败恢复**（产品设计文档 §3.2）。
2. **必填/格式校验**：活动名称（汉字字母数字，最多 20 字）；推广地址须 \`http://\` 或 \`https://\` 开头。
3. **提交**：\`POST /api/form/submit\`，body 为表单聚合对象；返回 \`code/message/data/requestId\`。
4. **失败保留输入**：校验或接口失败不丢失已填字段。
5. **重复提交防重**：提交中禁用按钮，避免双击重复 POST（错误码 409 时提示冲突）。
6. 底部：**上一步** / **下一步** / **提交** / 成功后 **再次创建**、**查看详情**。

### 业务定义

- 读写表：\`sample_form_record\`。

### 备注

> 验收（AT-03）：Given 用户填写表单，When 点击提交，Then 校验通过时提交成功，失败时保留输入并给出错误提示。

分组表单（GroupFormArco）同属 M03，规则与本页一致。`
  },
  5: {
    moduleName: '结果与异常页（M04 / SA-4）',
    markdown: `## 需求来源

**需求编号：SA-4** | **模块：M04**

### 涉及页面与路由

- 成功：\`/sample-app/result/success\`（SuccessPage）
- 失败：\`/sample-app/result/error\`（ErrorPage）
- 异常：\`/sample-app/exception/403\`、\`/404\`、\`/500\`（ForbiddenPage / NotFoundPage / ServerErrorPage）

### 显示样式

- 结果页：图标 + 主标题 + 副文案 + 进度/说明 + 操作按钮（如返回工作台）。
- 异常页：错误码、说明文案、返回首页或上一页入口。

### 交互与排序

1. 所有结果/异常页 **可访问**，路由直达无 404。
2. **回退路径可用**：提供返回工作台或项目列表等明确 CTA。
3. 可调用 \`GET /api/result/status\` 获取状态（契约摘要）；读写 \`result_log\`。
4. 异常场景覆盖：超时、断网、4xx、5xx 时需有补救动作（产品设计 §3.3）。

### 备注

> 验收（AT-04）：结果与异常页面可访问且回退路径可用。`
  },
  6: {
    moduleName: '子应用壳层与独立打包（M05 / SA-5）',
    markdown: `## 需求来源

**需求编号：SA-5** | **模块：M05** | **工程能力**

### 显示样式

- 侧栏菜单：多级分组（仪表盘、数据可视化、列表、表单、结果、异常、个人中心等）。
- 支持 **Hash 独立运行**（\`npm run dev:sample-app\`）与 **主站嵌入**（路由前缀 \`/sample-app\`）。
- 当前路由高亮、分组可折叠。

### 交互与排序

1. \`npm run build:sample-app\` 构建成功，产物目录 \`AIEP-WEB/dist/sample-app/\`。
2. 产物可通过 \`index.html\` / \`sample-app.html\` 直接访问（SA-5 验收）。
3. 菜单点击切换 \`router-view\` 内容区，不整页刷新。

### 业务定义

- 子应用编码：\`sample-app\`。
- 与主站路由映射见系统详细设计文档。

### 非功能

- **NFR-2**：需求-模块-接口-数据表可追溯（见 \`页面-路由-接口-数据表映射表.md\`）。
- **NFR-3**：请求失败可读提示，关键链路 \`requestId\` 可追踪。

### 备注

> 验收（AT-05）：独立打包产物可访问。`
  }
};
