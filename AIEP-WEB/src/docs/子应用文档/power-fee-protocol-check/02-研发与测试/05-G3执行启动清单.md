# power-fee-protocol-check G3执行启动清单

## 1. 启动目标（本周）

- 完成 `04-G3开发任务拆解表.md` 的负责人与排期字段落地。
- 启动第一批开发任务（M01~M04）并进入联调准备。
- 建立每日同步节奏，确保 `SA-F-API-DB-AT` 覆盖不漂移。

## 2. 今日必须完成项（D1）

- [x] 指定每条任务负责人（后端/前端/测试）。
- [x] 补齐每条任务计划开始、计划完成日期。
- [x] 将 `M01~M04` 状态改为 `in_progress`。
- [x] 锁定联调接口顺序：`API-01 -> API-03 -> API-04 -> API-02`。
- [x] 确认数据库脚本执行窗口与回滚人。

## 3. 首批执行范围（Iteration-1）

| 范围 | 模块 | 对应任务ID | 目标结果 |
|------|------|------|------|
| 核心配置链路 | M01 | DEV-M01-01 | 模板配置与版本快照可用 |
| 样本与规则链路 | M02/M03 | DEV-M02-01, DEV-M03-01 | 样本标注与规则发布可闭环 |
| 任务生成链路 | M04 | DEV-M04-01 | 批量任务可生成并可追踪 |

### 3.1 联调接口顺序与责任安排

| 顺序 | 接口 | 责任人 | 计划时段 | 依赖 |
|------|------|------|------|------|
| 1 | API-01（模板查询） | LTC | 20260428 AM | 无 |
| 2 | API-03（规则发布） | LTC | 20260428 PM | API-01 |
| 3 | API-04（任务生成） | LTC | 20260428 PM | API-03 |
| 4 | API-02（样本标注） | LTC | 20260428 PM | API-01 |

### 3.2 数据库脚本窗口与回滚安排

| 项目 | 内容 |
|------|------|
| 执行窗口 | 20260428 20:00-21:00 |
| 执行责任人 | LTC |
| 回滚责任人 | LTC |
| 回滚触发条件 | 建表失败、关键索引失败、任务主链路写入异常 |
| 回滚方式 | 按数据库设计文档DDL逆序回滚，恢复到执行前版本 |

## 4. 联调与提测入口条件

### 4.1 联调准入

- [ ] API 请求/响应结构与 PRD 契约一致。
- [ ] 关键错误码可复现（参数错误、幂等冲突、状态冲突）。
- [ ] 核心表写入字段与数据库设计文档一致。

### 4.2 提测准入（最小）

- [ ] `AT-01~AT-04` 有可执行脚本与证据路径。
- [ ] 任务状态链路可演示（创建 -> 执行 -> 完成/失败）。
- [ ] 关键异常具备恢复动作（重试、回滚、人工兜底）。

## 5. 日常节奏（建议）

- 每日站会：10 分钟，仅同步“进度、阻塞、风险”。
- 每日收口：更新 `04-G3开发任务拆解表.md` 状态与备注。
- 每两天联调：固定时段验证 API 与状态机一致性。
- 文档更新：仅在“人工明确提出更新要求”时执行。
- 轻量化执行：除任务拆解、联调/提测准入、风险阻塞外，不新增扩展文档。

## 5.1 当前执行结论

- 已进入开发环节：`M01~M04`（状态：`in_progress`）。
- 本阶段默认不扩写文档，优先代码实现与联调验证。

## 6. 当前阻塞登记（初始化）

| 阻塞ID | 描述 | 影响任务 | 责任人 | 预计解除时间 | 状态 |
|------|------|------|------|------|------|
| BLK-01 | 负责人与排期未落地 | 全部任务 | LTC | 20260428 | closed |
| BLK-02 | OCR依赖稳定性待验证 | DEV-M07-01, DEV-M08-01 | LTC | 20260428 | open |
