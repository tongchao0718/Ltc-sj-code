# 文档对齐清单

> **用途**：对照本仓库**当前实现**（代码与配置为单一事实来源），逐项核对根目录与 `docs/` 文档是否一致。  
> **维护**：功能、路由、脚本或打包策略变更后，请同步勾选/更新本清单对应行，并在相关文档中修订。

---

## 一、代码侧「权威参考」速查

| 类别 | 路径 / 文件 |
|------|-------------|
| 脚本命令 | `package.json` → `scripts` |
| 主应用构建 | `vite.config.js`、`index.html` |
| 子应用构建 | `build/vite.sample-app.config.js`、`build/sample-app.html` |
| 路由 | `src/router/index.js` |
| 全局样式 / 设计令牌 | `src/style.css`、`系统设计规范.md` |
| 顶栏与个人中心入口 | `src/App.vue` |
| 应用中心 | `src/views/AppCenter.vue` |
| 使用帮助 / 关于 | `src/views/HelpPage.vue`、`src/views/AboutPage.vue` |

---

## 二、`package.json` 脚本 ↔ 文档

| 检查项 | 当前实现 | 应对齐的文档 | 状态 |
|--------|----------|--------------|------|
| 主应用开发 | `npm run dev` | `README.md`、`使用说明.md`、`docs/系统文档/使用说明.md` | ☐ 核对端口说明（默认以终端输出为准） |
| 主应用构建 | `npm run build` | `README.md`、`主应用打包指南.md` | ☑ **`主应用打包指南.md`** 已按 `dist/` + 默认 Vite 配置修订（2026-04-02） |
| 预览 | `npm run preview` | `README.md`、使用说明类文档 | ☐ |
| 子应用开发 | `npm run dev:sample-app` | `README.md`、`子应用打包指南.md` | ☐ |
| 子应用构建 | `npm run build:sample-app` | `README.md`、`子应用打包指南.md` | ☐ |
| 一键更新 | `npm run update` | `一键更新指南.md`、`创建指南.md`（若引用） | ☐ 指南内示例代码若为 CommonJS/require，与现网 `scripts/update.js`（ESM）不一致，需统一描述 |
| 回滚 | `npm run rollback` | `一键更新指南.md` | ☐ 同上 |
| 版本输出 | `npm run version:echo` | 凡写 `npm run version` 的文档 | ☐ 本项目未使用保留字脚本 `version`，应写 `version:echo` 或删改文档 |
| `check-update` 等 | **未提供** | `产品设计文档.md`（若仍写）、`一键更新指南.md`（若仍写） | ☐ 删除或改为「未实现 / 规划中」，或补脚本 |
| Arco Design 依赖 | 已重新集成 | `package.json`、所有相关文档 | ☑ **`package.json`** 中已重新添加 Arco Design 依赖（2026-04-09） |

---

## 三、路由 ↔ 文档

**主应用（`createWebHistory`）当前路径：**

| 路径 | 名称 | 说明 |
|------|------|------|
| `/` | — | 重定向至 `/dashboard` |
| `/dashboard` | Dashboard | 工作台 |
| `/app-center` | AppCenter | 应用中心 |
| `/profile` | ProfileCenter | 个人中心（页） |
| `/review-center` | ReviewCenter | 项目审查 |
| `/help` | Help | 使用帮助（**独立页**，非弹窗） |
| `/about` | About | 关于我们（**独立页**，非弹窗） |
| `/sample-app/*` | SampleApp | 示例子应用嵌套路由 |

| 检查项 | 文档中常见错误写法 | 应对齐为 | 状态 |
|--------|-------------------|----------|------|
| 应用中心路径 | `/apps` | `/app-center` | ☑ **`创建指南.md`** 已修订（§1.2、§4.2.3 路由全文、§4.2.4 废弃说明）；`产品设计文档` 等仍待修订 |
| 使用帮助 / 关于 | 仅描述弹窗 + `HelpModal` / `AboutModal` | **路由页** `/help`、`/about`；顶栏为 `router-link` | ☑ **`创建指南.md`** `App.vue` 示例与 §4.2.3.1/§4.2.3.2/§4.5 已标注「现网为页面」；`产品设计文档` 等仍待修订 |
| 个人中心 | 与「个人资料」混用 | 主系统：`/profile`；子应用内：`/sample-app/profile` | ☐ |

**示例子应用子路由（嵌入主应用时带 `/sample-app` 前缀）：**  
与 [Arco Design Pro Vue 官方路由模块](https://github.com/arco-design/arco-design-pro-vue/tree/main/arco-design-pro-vite/src/router/routes/modules) 一致：`dashboard/workplace|monitor`、`visualization/*`、`list/*`、`form/*`、`profile/basic`、`result/*`、`exception/403|404|500`、`user/info|setting`；旧路径按需重定向至 `dashboard/workplace` 或 `user/*`。

| 检查项 | 状态 |
|--------|------|
| `创建指南` 中示例路由是否包含 `profile`、`settings`、`protocol` 与现网一致 | ☑ **§4.2.3** 已与 `src/router/index.js` 全文对齐 |
| 首页「页面数量」等统计是否与 `src/router/index.js` 一致（当前主系统可统计页为 **6** + 子应用 **16** = **22**，以 `Dashboard.vue` 注释为准） | ☐ |

---

## 四、界面与产品描述 ↔ `产品设计文档.md`

| 检查项 | 当前实现 | 文档建议修订方向 | 状态 |
|--------|----------|------------------|------|
| 应用中心布局 | 卡片列表 + `folder` 目录名（如 `sample-app`），**非固定 3×3 九宫格** | 删除或改为「可扩展网格 / 当前为单应用示例」 | ☐ |
| 顶栏风格 | **浅色顶栏** + Arco 系令牌（见 `系统设计规范` 与 `style.css`） | 若仍写「整栏蓝色主题」，改为与现网一致或标注为历史方案 | ☐ |
| 使用帮助 / 关于 | **页面** + 返回链接 | 与「弹窗」相关章节合并或改写 | ☐ |
| 个人中心下拉 | 含：个人资料、使用帮助、关于我们、项目审查 | 核对与 `App.vue` 菜单项一致 | ☐ |

---

## 五、`系统设计规范.md` ↔ `src/style.css` / 组件

| 检查项 | 状态 |
|--------|------|
| 主色 #165DFF 及浅色/深色变量是否与 `style.css` 中 `:root` / `[data-theme]` 一致 | ☐ |
| 圆角、阴影、字号阶梯是否在文档「示例 HTML」与 `.ds-card`、`.btn-*` 中无矛盾 | ☐ |
| 文档中「可选集成 Arco Design Vue」与当前「纯 CSS 变量」实现是否说明清楚 | ☐ |

---

## 六、`主应用打包指南.md` ↔ `vite.config.js`

| 检查项 | 当前实现 | 文档常见描述 | 状态 |
|--------|----------|--------------|------|
| 输出目录 | **`dist/`**（根目录 `index.html`） | 部分章节写 **`dist/main`** | ☑ 文档已改为 **`dist/`** |
| `base` | 默认 `/`（开发/典型部署） | 文档可能写 `./` + IIFE | ☑ 已说明默认 `/` 与子路径部署 |
| 主应用产物格式 | **ES 模块分包**（Vite 默认） | 文档可能写 **IIFE**、`manualChunks` 等 | ☑ 已明确主应用为 ESM，与子应用 IIFE 区分 |
| `start.html` / CDN 替换 | 主应用构建**未实现**指南中的插件逻辑 | ☑ 已在 **`主应用打包指南.md`** 标注主应用不生成 `start.html`；子应用见 **`子应用打包指南.md`** |

---

## 七、`子应用打包指南.md` ↔ `build/vite.sample-app.config.js`

| 检查项 | 状态 |
|--------|------|
| `base: './'`、`createWebHashHistory`、产物目录 `dist/sample-app/` | ☐ |
| `copyHtmlPlugin` 行为与 `index.html` / `sample-app.html` / `start.html` 是否一致 | ☐ |
| 示例中 `favicon` 写 `vite.svg` 与现网 `public/app-icon.png` | ☐ 示例改为 `app-icon.png` 或写「以 `public/` 为准」 |
| `inlineDynamicImports` + IIFE 等是否与配置文件一致 | ☐ |

---

## 八、`创建指南.md`（大文件）专项

| 检查项 | 建议动作 | 状态 |
|--------|----------|------|
| 全文检索 `/apps` → 按语境改为 `/app-center` 或说明别名 | ☑ 主导航、§4.2.3、§4.2.5 卡片链接等已改；**`src/apps` 目录名保留** |
| `HelpModal` / `AboutModal` / `SettingsModal` 整段示例 | 改为 **`HelpPage`/`AboutPage` 路由** 或注明「历史方案」 | ☑ **§4.2.3.1 / §4.2.3.2 / §4.5** 已标注可选与非主路径 |
| 示例 `App.vue` 与现网 `App.vue`（品牌区、`app-icon`、主题） | 增加「与仓库对照」说明或拆出「附录：当前仓库快照」 | ☑ **§4.2.2** 已换为路由版骨架并指向仓库全文 |
| 环境脚本 `install-env.sh` 中误用 `try/catch`（bash 无效语法） | ☐ 修订为合法 shell 或标注勿直接复制 |

---

## 九、`README.md` ↔ 现网

| 检查项 | 状态 |
|--------|------|
| 项目结构树是否包含 `docs/`、`public/app-icon.png`、`views/HelpPage`、`AboutPage`、`utils/theme.js` | ☐ |
| 快速开始命令与 `package.json` 一致 | ☐ |
| 「常见问题」中子应用本地打开是否与 Hash + `dist/sample-app` 一致 | ☐ |

---

## 十、`docs/系统文档/*` 与 `docs/子应用文档/sample-app/*`

| 检查项 | 状态 |
|--------|------|
| `目录结构说明.md`：是否反映 `HelpPage`/`AboutPage`、删除九宫格占位描述 | ☐ |
| `详细设计文档.md` / `概要设计文档.md`：路由表与顶栏行为 | ☐ |
| `使用说明.md`：帮助/关于为页面路径 | ☐ |
| 子应用文档：侧栏含「个人资料」「设置」路由 | ☐ |

---

## 十一、示例应用界面修改

| 检查项 | 当前实现 | 应对齐的文档 | 状态 |
|--------|----------|--------------|------|
| Arco Design 组件 | 已替换为原生 HTML 元素 | 所有相关文档 | ☑ **所有示例应用页面** 已移除 Arco Design 组件，替换为原生 HTML 元素（2026-04-09） |
| 样式 | 使用具体颜色值替代 CSS 变量 | 所有相关文档 | ☑ **所有示例应用页面** 已更新样式，使用具体颜色值替代 CSS 变量（2026-04-09） |
| 图标 | 使用 Unicode 图标替代 Arco 图标 | 所有相关文档 | ☑ **所有示例应用页面** 已使用 Unicode 图标替代 Arco 图标（2026-04-09） |
| 国际化 | 已移除 i18n 依赖 | 所有相关文档 | ☑ **示例应用** 已移除 i18n 依赖，使用硬编码中文文本（2026-04-09） |

---

## 十二、代码资产清理（文档若仍引用则需同步）

| 资产 | 现状 | 建议 |
|------|------|------|
| `src/components/HelpModal.vue`、`AboutModal.vue` | 可能已无引用 | ☐ 删除文件并在所有文档中去掉弹窗表述，或恢复弹窗用法 |
| `public/vite.svg` | 可选保留 | ☐ 文档勿再写「默认系统图标」若已统一为 `app-icon.png` |

---

## 十三、评审通过标准（建议）

- [ ] 上表所有「状态」列为关键文档（README、创建指南、产品设计、主/子打包指南、一键更新、系统设计规范）已勾选完成或明确标注「刻意保留差异」及原因。  
- [ ] 任意新增路由 / 脚本 / 打包目录，已同步 **本清单** + **至少一处** 用户面向说明（README 或 `docs/系统文档/使用说明.md`）。  
- [ ] AI/团队「从零创建」路径：以 **本清单第一节** 为入口，避免以过期长文档片段为唯一依据。
- [ ] 示例应用界面修改已在相关文档中同步说明。

---

**文档版本**：与仓库 `package.json` version 或提交日期同步更新本段。  
**最后更新**：由维护人在每次发版前修订。

---

### 修订记录（摘要）

| 日期 | 内容 |
|------|------|
| 2026-04-09 | 示例应用界面修改：重新集成 Arco Design 依赖；使用 Arco Design 组件和样式；使用 Arco 图标；重新集成 i18n 依赖 |
| 2026-04-02 | 高优先级对齐：`创建指南.md`（§1.2、`App.vue`、`router`、废弃重复路由节、Help/About 说明）、`主应用打包指南.md`（`dist/`、真实 `vite.config`、`index.html`）、`环境安装指南.md`（Node 18+） |
| 2026-04-02 | 轻量团队入口：新增 **`快速初始化.md`**、**`AGENTS.md`**、**`.cursor/rules/ltc-framework.mdc`**；`README.md` 顶栏改为「克隆优先」 |
