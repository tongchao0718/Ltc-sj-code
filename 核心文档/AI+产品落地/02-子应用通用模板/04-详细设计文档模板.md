# <子应用名称> 详细设计文档

## 1. 组件结构

- 入口组件：`<app-entry-file>`
- 页面目录：`views/`
- 公共组件：`components/`
- 工具目录：`utils/`
- 样式目录：`styles/`
- 国际化目录：`locales/`（如适用）

## 2. 路由对照

| 场景 | 路径示例 | 页面组件 |
|------|------|------|
| 主应用嵌套 | `<主路由前缀>/module/page` | `<Page.vue>` |
| 独立运行 | `#/module/page` | `<Page.vue>` |

## 3. 样式与主题

- 主题变量来源：<全局变量文件>
- 局部样式策略：<scoped/module css>
- 响应式策略：<断点、侧栏折叠规则>

## 4. 功能特性实现说明

| 模块 | 关键能力 | 实现要点 |
|------|------|------|
| <模块1> | <能力> | <实现说明> |
| <模块2> | <能力> | <实现说明> |

> 强制要求：本章禁止“一句话描述”。每个模块至少补齐“输入、处理、输出、异常与恢复、验收”五类信息，且每类至少 1 条可执行描述。

### 4.1 模块实现清单（强制）

| 模块 | 输入 | 处理逻辑 | 输出 | 异常与恢复 | 验收 |
|------|------|------|------|------|------|
| <模块1> | <输入> | <处理> | <输出> | <异常/恢复> | <AT-ID> |
| <模块2> | <输入> | <处理> | <输出> | <异常/恢复> | <AT-ID> |

## 5. 关键页面时序（最小实现）

### 5.1 <页面A>

1. 页面初始化：<动作>
2. 用户操作：<动作>
3. 请求发送：<动作>
4. 结果处理：<动作>
5. 后续动作：<动作>

### 5.2 <页面B>

1. 页面初始化：<动作>
2. 用户操作：<动作>
3. 请求发送：<动作>
4. 结果处理：<动作>
5. 后续动作：<动作>

## 6. 状态机与异常处理（最小实现）

### 6.1 通用状态

- `idle`：初始态
- `loading`：请求中
- `success`：成功渲染
- `empty`：成功但无数据
- `error`：失败态

### 6.2 异常处理策略

| 场景 | 触发条件 | 处理方式 | 用户可见反馈 |
|------|------|------|------|
| 参数错误 | <条件> | <处理> | <提示> |
| 权限不足 | <条件> | <处理> | <提示> |
| 服务异常 | <条件> | <处理> | <提示> |

### 6.3 标准编码覆盖说明（强制）

| 状态/类型 | 是否纳入标准编码 | 编码类型 | 备注 |
|------|------|------|------|
| <任务状态> | 是/否 | `<TASK_STATUS>` | <说明> |
| <工单状态> | 是/否 | `<ISSUE_STATUS>` | <说明> |

> 强制要求：本章节需与 `数据库设计文档` 的“标准编码清单”保持一致；若某状态未纳入标准编码，必须明确写“否”及原因，禁止留空。

## 7. 设计与实现边界

- 需求与验收口径以 `需求说明书.md` 为准。
- 产品信息架构与交互原则以 `产品设计文档.md` 为准。
- 接口契约、数据模型、DoD 以 `PRD设计评审文档.md` 为准。
- 字段定义、标准编码名与物理模型以 `数据库设计文档.md` 为准。

> 强制要求：若文档口径冲突，必须记录“冲突项 + 以谁为准 + 同步回写动作”，否则视为未完成详细设计闭环。
