# 系统更新分发方案

## 1. 方案概述

本方案提供了一套完整的系统更新分发机制，允许开发者将新升级的全量程序包分发给其他已安装的用户，用户可以通过一键脚本快速合并更新到最新版本。

## 2. 更新流程

### 2.1 开发者端流程

1. **准备更新包**
2. **打包发布**
3. **分发更新包**
4. **用户下载更新**

### 2.2 用户端流程

1. **下载更新包**
2. **执行更新脚本**
3. **验证更新结果**
4. **回滚（如需要）**

## 3. 更新包结构

### 3.1 更新包目录结构

```
ltc-demo-update-v1.2.0/
├── package.json                    # 更新包信息
├── update-info.json                # 更新详情信息
├── update.js                       # 一键更新脚本
├── rollback.js                     # 一键回滚脚本
├── files/                          # 更新文件目录
│   ├── src/                        # 源代码文件
│   ├── public/                     # 公共资源文件
│   ├── build/                      # 构建配置文件
│   ├── scripts/                    # 脚本文件
│   ├── docs/                       # 文档文件
│   ├── 框架核心文档/                 # 框架核心文档
│   ├── package.json                # 项目依赖配置
│   ├── package-lock.json           # 依赖锁定文件
│   └── ...                         # 其他项目文件
├── dist/                           # 构建产物（可选）
│   ├── index.html
│   ├── assets/
│   └── sample-app/
└── README.md                       # 更新说明文档
```

### 3.2 更新包信息文件

#### package.json（更新包）

```json
{
  "name": "ltc-demo-update",
  "version": "1.2.0",
  "updateType": "full",
  "targetVersion": "1.2.0",
  "minCompatibleVersion": "1.0.0",
  "releaseDate": "2026-04-10",
  "description": "LTC 应用系统 v1.2.0 全量更新包",
  "author": "LTC Team",
  "dependencies": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
```

#### update-info.json

```json
{
  "version": "1.2.0",
  "previousVersion": "1.1.0",
  "releaseDate": "2026-04-10",
  "updateType": "full",
  "updateMode": "merge",
  "fileCount": 150,
  "totalSize": "25.6MB",
  "changes": [
    {
      "type": "feature",
      "category": "新增功能",
      "description": "集成 Pinia 状态管理库"
    },
    {
      "type": "feature",
      "category": "新增功能",
      "description": "集成 Vue I18n 国际化支持"
    },
    {
      "type": "feature",
      "category": "新增功能",
      "description": "集成 ECharts 数据可视化"
    },
    {
      "type": "improvement",
      "category": "优化改进",
      "description": "优化 Arco Design 组件集成"
    },
    {
      "type": "bugfix",
      "category": "问题修复",
      "description": "修复 Pinia 未注册导致页面空白的问题"
    }
  ],
  "requirements": {
    "nodeVersion": ">=18.0.0",
    "npmVersion": ">=8.0.0",
    "diskSpace": "100MB"
  },
  "backupRequired": true,
  "restartRequired": true
}
```

## 4. 更新脚本实现

### 4.1 打包脚本（开发者端）

创建 `scripts/create-update-package.js`：

```javascript
import fs from 'fs'
import path from 'path'
import { execSync } from 'child_process'
import { fileURLToPath } from 'url'
import archiver from 'archiver'

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const projectRoot = path.join(__dirname, '..')

async function createUpdatePackage() {
  console.log('开始创建更新包...')

  try {
    // 1. 读取当前版本
    const packageJson = JSON.parse(fs.readFileSync(path.join(projectRoot, 'package.json'), 'utf-8'))
    const currentVersion = packageJson.version
    console.log(`当前版本: ${currentVersion}`)

    // 2. 构建项目
    console.log('构建项目...')
    execSync('npm run build', { stdio: 'inherit', cwd: projectRoot })
    execSync('npm run build:sample-app', { stdio: 'inherit', cwd: projectRoot })

    // 3. 创建更新包目录
    const updatePackageName = `ltc-demo-update-v${currentVersion}`
    const updatePackageDir = path.join(projectRoot, 'dist', updatePackageName)
    
    if (fs.existsSync(updatePackageDir)) {
      fs.rmSync(updatePackageDir, { recursive: true, force: true })
    }
    fs.mkdirSync(updatePackageDir, { recursive: true })

    // 4. 复制必要文件
    console.log('复制文件...')
    const filesToCopy = [
      'src',
      'public',
      'build',
      'scripts',
      'docs',
      'package.json',
      'package-lock.json',
      'vite.config.js',
      'index.html'
    ]

    for (const file of filesToCopy) {
      const sourcePath = path.join(projectRoot, file)
      const destPath = path.join(updatePackageDir, file)
      
      if (fs.existsSync(sourcePath)) {
        if (fs.statSync(sourcePath).isDirectory()) {
          copyDirectory(sourcePath, destPath)
        } else {
          fs.copyFileSync(sourcePath, destPath)
        }
        console.log(`  ✓ ${file}`)
      }
    }

    // 5. 创建更新包信息文件
    const updateInfo = {
      version: currentVersion,
      releaseDate: new Date().toISOString().split('T')[0],
      updateType: 'full',
      updateMode: 'merge',
      changes: getChangesFromGit(),
      requirements: {
        nodeVersion: '>=18.0.0',
        npmVersion: '>=8.0.0',
        diskSpace: '100MB'
      },
      backupRequired: true,
      restartRequired: true
    }

    fs.writeFileSync(
      path.join(updatePackageDir, 'update-info.json'),
      JSON.stringify(updateInfo, null, 2)
    )

    // 6. 创建更新包的 package.json
    const updatePackageJson = {
      name: 'ltc-demo-update',
      version: currentVersion,
      updateType: 'full',
      targetVersion: currentVersion,
      minCompatibleVersion: '1.0.0',
      releaseDate: new Date().toISOString().split('T')[0],
      description: `LTC 应用系统 v${currentVersion} 全量更新包`,
      author: 'LTC Team',
      dependencies: {
        node: '>=18.0.0',
        npm: '>=8.0.0'
      }
    }

    fs.writeFileSync(
      path.join(updatePackageDir, 'package.json'),
      JSON.stringify(updatePackageJson, null, 2)
    )

    // 7. 创建更新脚本
    createUpdateScript(updatePackageDir, currentVersion)
    createRollbackScript(updatePackageDir, currentVersion)

    // 8. 创建 README
    createReadme(updatePackageDir, currentVersion)

    // 9. 打包为 ZIP 文件
    console.log('打包更新包...')
    const zipFileName = `${updatePackageName}.zip`
    const zipFilePath = path.join(projectRoot, 'dist', zipFileName)
    
    await createZipFile(updatePackageDir, zipFilePath)

    // 10. 清理临时目录
    fs.rmSync(updatePackageDir, { recursive: true, force: true })

    console.log(`\n✅ 更新包创建成功！`)
    console.log(`📦 更新包路径: ${zipFilePath}`)
    console.log(`📏 文件大小: ${(fs.statSync(zipFilePath).size / 1024 / 1024).toFixed(2)} MB`)

  } catch (error) {
    console.error('❌ 创建更新包失败:', error.message)
    process.exit(1)
  }
}

function copyDirectory(src, dest) {
  fs.mkdirSync(dest, { recursive: true })
  const entries = fs.readdirSync(src, { withFileTypes: true })

  for (const entry of entries) {
    const srcPath = path.join(src, entry.name)
    const destPath = path.join(dest, entry.name)

    if (entry.isDirectory()) {
      copyDirectory(srcPath, destPath)
    } else {
      fs.copyFileSync(srcPath, destPath)
    }
  }
}

function getChangesFromGit() {
  try {
    const lastTag = execSync('git describe --tags --abbrev=0 2>/dev/null || echo ""', {
      encoding: 'utf-8'
    }).trim()

    let logCommand = 'git log --pretty=format:"%h|%s|%an|%ad" --date=short -10'
    if (lastTag) {
      logCommand = `git log ${lastTag}..HEAD --pretty=format:"%h|%s|%an|%ad" --date=short`
    }

    const logs = execSync(logCommand, { encoding: 'utf-8' }).trim().split('\n')

    return logs.map(log => {
      const [hash, message, author, date] = log.split('|')
      return {
        type: message.includes('feat') ? 'feature' : 
              message.includes('fix') ? 'bugfix' : 'improvement',
        category: message.includes('feat') ? '新增功能' : 
                 message.includes('fix') ? '问题修复' : '优化改进',
        description: message,
        author,
        date
      }
    })
  } catch (error) {
    return [{
      type: 'improvement',
      category: '优化改进',
      description: '系统更新',
      author: 'LTC Team',
      date: new Date().toISOString().split('T')[0]
    }]
  }
}

function createUpdateScript(packageDir, version) {
  const scriptContent = `#!/usr/bin/env node

import fs from 'fs'
import path from 'path'
import { execSync } from 'child_process'
import { fileURLToPath } from 'url'

const __filename = fileURLToPath(import.meta.url)
const __dirname = path.dirname(__filename)
const projectRoot = path.join(__dirname, '..')

console.log('开始系统更新...')
console.log(\`更新版本: v\${version}\`)

try {
  // 1. 检查环境
  console.log('\\n检查环境...')
  const nodeVersion = process.version
  console.log(\`  Node.js 版本: \${nodeVersion}\`)
  
  if (nodeVersion < 'v18.0.0') {
    console.error('❌ Node.js 版本过低，需要 18.0 或更高版本')
    process.exit(1)
  }

  // 2. 创建备份
  console.log('\\n创建备份...')
  const timestamp = new Date().toISOString().replace(/[:.]/g, '-')
  const backupDir = path.join(projectRoot, 'backups', \`v\${version}-\${timestamp}\`)
  fs.mkdirSync(backupDir, { recursive: true })
  
  const filesToBackup = ['src', 'public', 'build', 'scripts', 'docs', 'package.json', 'package-lock.json']
  for (const file of filesToBackup) {
    const sourcePath = path.join(projectRoot, file)
    if (fs.existsSync(sourcePath)) {
      const destPath = path.join(backupDir, file)
      if (fs.statSync(sourcePath).isDirectory()) {
        copyDirectory(sourcePath, destPath)
      } else {
        fs.copyFileSync(sourcePath, destPath)
      }
      console.log(\`  ✓ 备份 \${file}\`)
    }
  }

  // 3. 复制更新文件
  console.log('\\n复制更新文件...')
  const updateFilesDir = path.join(__dirname, 'files')
  const filesToUpdate = ['src', 'public', 'build', 'scripts', 'docs', 'package.json', 'package-lock.json']
  
  for (const file of filesToUpdate) {
    const sourcePath = path.join(updateFilesDir, file)
    const destPath = path.join(projectRoot, file)
    
    if (fs.existsSync(sourcePath)) {
      if (fs.existsSync(destPath)) {
        if (fs.statSync(destPath).isDirectory()) {
          fs.rmSync(destPath, { recursive: true, force: true })
        } else {
          fs.unlinkSync(destPath)
        }
      }
      
      if (fs.statSync(sourcePath).isDirectory()) {
        copyDirectory(sourcePath, destPath)
      } else {
        fs.copyFileSync(sourcePath, destPath)
      }
      console.log(\`  ✓ 更新 \${file}\`)
    }
  }

  // 4. 安装依赖
  console.log('\\n安装依赖...')
  execSync('npm install', { stdio: 'inherit', cwd: projectRoot })

  // 5. 构建项目
  console.log('\\n构建项目...')
  execSync('npm run build', { stdio: 'inherit', cwd: projectRoot })
  execSync('npm run build:sample-app', { stdio: 'inherit', cwd: projectRoot })

  // 6. 清理更新包
  console.log('\\n清理更新包...')
  const updatePackageDir = path.join(projectRoot, 'ltc-demo-update')
  if (fs.existsSync(updatePackageDir)) {
    fs.rmSync(updatePackageDir, { recursive: true, force: true })
  }

  console.log('\\n✅ 系统更新完成！')
  console.log('\\n📝 更新内容：')
  console.log(\`  - 更新到版本 v\${version}\`)
  console.log('  - 已创建更新备份')
  console.log('  - 已安装最新依赖')
  console.log('  - 已重新构建项目')
  console.log('\\n💡 提示：')
  console.log(\`  - 备份位置: \${backupDir}\`)
  console.log('  - 如果更新后出现问题，可以使用 rollback.js 回滚')
  console.log('  - 建议重启开发服务器: npm run dev')

} catch (error) {
  console.error('\\n❌ 更新失败:', error.message)
  console.error('\\n💡 提示：')
  console.error('  - 请检查错误信息并重试')
  console.error('  - 如果问题持续，请联系技术支持')
  process.exit(1)
}

function copyDirectory(src, dest) {
  fs.mkdirSync(dest, { recursive: true })
  const entries = fs.readdirSync(src, { withFileTypes: true })

  for (const entry of entries) {
    const srcPath = path.join(src, entry.name)
    const destPath = path.join(dest, entry.name)

    if (entry.isDirectory()) {
      copyDirectory(srcPath, destPath)
    } else {
      fs.copyFileSync(srcPath, destPath)
    }
  }
}
`

  fs.writeFileSync(path.join(packageDir, 'update.js'), scriptContent)
}

function createRollbackScript(packageDir, version) {
  const scriptContent = `#!/usr/bin/env node

import fs from 'fs'
import path from 'path'
import { execSync } from 'child_process'
import readline from 'readline'
import { fileURLToPath } from 'url'

const __filename = fileURLToPath(import.meta.url)
const __dirname = path.dirname(__filename)
const projectRoot = path.join(__dirname, '..')

console.log('开始版本回滚...')

try {
  // 1. 查找备份
  console.log('\\n查找备份...')
  const backupsDir = path.join(projectRoot, 'backups')
  
  if (!fs.existsSync(backupsDir)) {
    console.error('❌ 未找到备份目录')
    process.exit(1)
  }

  const backups = fs.readdirSync(backupsDir)
    .filter(dir => dir.startsWith(\`v\${version}-\`))
    .sort()
    .reverse()

  if (backups.length === 0) {
    console.error(\`❌ 未找到版本 v\${version} 的备份\`)
    process.exit(1)
  }

  console.log('\\n可用的备份：')
  backups.forEach((backup, index) => {
    console.log(\`  \${index + 1}. \${backup}\`)
  })

  const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
  })

  rl.question(\`\\n请选择要回滚的备份 (1-\${backups.length}): \`, (answer) => {
    try {
      const choice = parseInt(answer.trim()) - 1
      if (choice < 0 || choice >= backups.length) {
        console.error('❌ 无效的选择')
        rl.close()
        process.exit(1)
        return
      }

      const selectedBackup = backups[choice]
      const backupPath = path.join(backupsDir, selectedBackup)

      console.log(\`\\n回滚到备份: \${selectedBackup}\`)

      // 2. 恢复文件
      console.log('\\n恢复文件...')
      const filesToRestore = ['src', 'public', 'build', 'scripts', 'docs', 'package.json', 'package-lock.json']
      
      for (const file of filesToRestore) {
        const sourcePath = path.join(backupPath, file)
        const destPath = path.join(projectRoot, file)
        
        if (fs.existsSync(sourcePath)) {
          if (fs.existsSync(destPath)) {
            if (fs.statSync(destPath).isDirectory()) {
              fs.rmSync(destPath, { recursive: true, force: true })
            } else {
              fs.unlinkSync(destPath)
            }
          }
          
          if (fs.statSync(sourcePath).isDirectory()) {
            copyDirectory(sourcePath, destPath)
          } else {
            fs.copyFileSync(sourcePath, destPath)
          }
          console.log(\`  ✓ 恢复 \${file}\`)
        }
      }

      // 3. 安装依赖
      console.log('\\n安装依赖...')
      execSync('npm install', { stdio: 'inherit', cwd: projectRoot })

      // 4. 构建项目
      console.log('\\n构建项目...')
      execSync('npm run build', { stdio: 'inherit', cwd: projectRoot })
      execSync('npm run build:sample-app', { stdio: 'inherit', cwd: projectRoot })

      console.log('\\n✅ 版本回滚完成！')
      console.log('\\n📝 回滚内容：')
      console.log(\`  - 回滚到备份: \${selectedBackup}\`)
      console.log('  - 已恢复文件')
      console.log('  - 已重新安装依赖')
      console.log('  - 已重新构建项目')
      console.log('\\n💡 提示：')
      console.log('  - 建议重启开发服务器: npm run dev')

    } catch (error) {
      console.error('❌ 回滚失败:', error.message)
    } finally {
      rl.close()
    }
  })

} catch (error) {
  console.error('❌ 回滚失败:', error.message)
  process.exit(1)
}

function copyDirectory(src, dest) {
  fs.mkdirSync(dest, { recursive: true })
  const entries = fs.readdirSync(src, { withFileTypes: true })

  for (const entry of entries) {
    const srcPath = path.join(src, entry.name)
    const destPath = path.join(dest, entry.name)

    if (entry.isDirectory()) {
      copyDirectory(srcPath, destPath)
    } else {
      fs.copyFileSync(srcPath, destPath)
    }
  }
}
`

  fs.writeFileSync(path.join(packageDir, 'rollback.js'), scriptContent)
}

function createReadme(packageDir, version) {
  const readmeContent = `# LTC 应用系统 v${version} 更新包

## 更新说明

本更新包用于将 LTC 应用系统更新到 v${version} 版本。

## 更新内容

- 集成 Pinia 状态管理库
- 集成 Vue I18n 国际化支持
- 集成 ECharts 数据可视化
- 优化 Arco Design 组件集成
- 修复 Pinia 未注册导致页面空白的问题

## 系统要求

- Node.js: >= 18.0.0
- npm: >= 8.0.0
- 磁盘空间: 100MB

## 更新步骤

### 1. 解压更新包

将更新包解压到项目根目录。

### 2. 执行更新脚本

\`\`\`bash
node update.js
\`\`\`

### 3. 验证更新

更新完成后，启动开发服务器验证：

\`\`\`bash
npm run dev
\`\`\`

### 4. 回滚（如需要）

如果更新后出现问题，可以执行回滚脚本：

\`\`\`bash
node rollback.js
\`\`\`

## 注意事项

- 更新前会自动创建备份
- 更新过程中请勿关闭终端
- 更新完成后建议重启开发服务器
- 如遇问题，请查看备份目录中的文件

## 技术支持

如遇到问题，请联系技术支持团队。

---

更新日期: ${new Date().toISOString().split('T')[0]}
版本: v${version}
`

  fs.writeFileSync(path.join(packageDir, 'README.md'), readmeContent)
}

function createZipFile(sourceDir, outputPath) {
  return new Promise((resolve, reject) => {
    const output = fs.createWriteStream(outputPath)
    const archive = archiver('zip', { zlib: { level: 9 } })

    output.on('close', () => {
      console.log(`  ✓ ZIP 文件创建完成: ${archive.pointer()} bytes`)
      resolve()
    })

    archive.on('error', (err) => {
      reject(err)
    })

    archive.pipe(output)
    archive.directory(sourceDir, false)
    archive.finalize()
  })
}

createUpdatePackage()
```

### 4.2 更新脚本（用户端）

更新脚本会在打包时自动生成，包含以下功能：
- 环境检查
- 自动备份
- 文件更新
- 依赖安装
- 项目构建

### 4.3 回滚脚本（用户端）

回滚脚本会在打包时自动生成，包含以下功能：
- 备份列表显示
- 备份选择
- 文件恢复
- 依赖重装
- 项目重构建

## 5. 分发方式

### 5.1 本地分发

1. **文件共享**
   - 通过 U 盘、网络共享等方式分发 ZIP 文件
   - 用户解压后执行更新脚本

2. **内网服务器**
   - 将更新包上传到内网文件服务器
   - 提供下载链接给用户

### 5.2 在线分发

1. **CDN 分发**
   - 将更新包上传到 CDN
   - 提供稳定的下载链接

2. **自动更新**
   - 在应用中集成自动更新检查
   - 检测到新版本时自动下载并更新

## 6. 使用流程

### 6.1 开发者创建更新包

```bash
# 安装打包依赖
npm install --save-dev archiver

# 创建更新包
node scripts/create-update-package.js
```

### 6.2 用户执行更新

```bash
# 1. 下载并解压更新包
unzip ltc-demo-update-v1.2.0.zip

# 2. 进入更新包目录
cd ltc-demo-update-v1.2.0

# 3. 执行更新脚本
node update.js

# 4. 验证更新
cd ..
npm run dev
```

### 6.3 用户回滚版本

```bash
# 1. 进入更新包目录
cd ltc-demo-update-v1.2.0

# 2. 执行回滚脚本
node rollback.js

# 3. 选择备份
# 按照提示选择要回滚的备份

# 4. 验证回滚
cd ..
npm run dev
```

## 7. 版本管理

### 7.1 版本号规范

遵循语义化版本规范（Semantic Versioning）：
- 主版本号（MAJOR）：不兼容的 API 修改
- 次版本号（MINOR）：向下兼容的功能性新增
- 修订号（PATCH）：向下兼容的问题修正

### 7.2 版本更新记录

每次更新时，更新包会包含：
- 版本号
- 更新日期
- 更新内容
- 兼容性信息
- 依赖要求

## 8. 安全考虑

### 8.1 文件完整性

- 更新包应包含校验和（MD5/SHA256）
- 用户在解压前应验证文件完整性

### 8.2 备份机制

- 更新前自动创建备份
- 备份包含所有关键文件
- 支持多版本备份管理

### 8.3 权限控制

- 更新脚本需要适当的文件系统权限
- 建议在非生产环境先测试更新

## 9. 故障处理

### 9.1 更新失败

1. 检查错误信息
2. 验证环境要求
3. 检查磁盘空间
4. 重新执行更新脚本

### 9.2 回滚失败

1. 检查备份文件完整性
2. 验证文件权限
3. 手动恢复备份文件
4. 联系技术支持

### 9.3 构建失败

1. 检查依赖安装
2. 清理缓存：`npm cache clean --force`
3. 重新安装依赖：`rm -rf node_modules package-lock.json && npm install`
4. 重新构建

## 10. 文档变更管理

### 10.1 文档变更流程

1. **文档识别**
   - 识别所有需要更新的文档文件
   - 包括但不限于：产品设计文档、安装指南、框架基线、系统设计规范、文档对齐清单、更新分发方案、一键更新指南、主应用打包指南、子应用打包指南、创建指南等
   - **框架核心文档**：必须包含框架核心文档目录中的所有文档

2. **文档更新**
   - 在代码更新的同时同步更新相关文档
   - 确保文档内容与代码实现保持一致
   - 更新文档中的版本号、功能说明、安装步骤等信息

3. **文档验证**
   - 验证文档的准确性和完整性
   - 确保文档与实际功能匹配
   - 检查文档格式和结构是否规范

4. **文档分发**
   - 将更新后的文档包含在更新包中
   - 确保用户能够获取到最新的文档

### 10.2 文档变更要求

1. **版本一致性**
   - 文档版本应与代码版本保持一致
   - 每次代码更新时应同步更新文档版本号

2. **内容完整性**
   - 文档应包含所有新功能的说明
   - 应更新所有相关的安装、配置和使用说明

3. **格式规范**
   - 文档应遵循统一的格式规范
   - 应使用清晰的标题层级和结构

4. **可追溯性**
   - 文档变更应与代码变更保持一致
   - 应记录文档变更的原因和内容

### 10.3 变更文档打包说明

1. **变更文档检测**
   - 使用 Git 版本控制工具检测变更的文档文件
   - 只打包变更的文档文件，减少更新包大小
   - 首次打包时包含所有文档文件

2. **变更文档列表**
   - 在更新包中包含变更文档的列表
   - 清晰标识哪些文档发生了变更
   - 提供文档变更的简要说明

3. **文档打包流程**
   - 自动检测变更的文档文件
   - 将变更的文档文件添加到更新包
   - **框架核心文档**：必须将框架核心文档目录中的所有文档包含在更新包中
   - 确保文档文件的完整性和正确性

### 10.4 更新内容说明文档要求

1. **文档内容**
   - 详细说明版本更新内容
   - 包含新增功能、优化改进、问题修复等信息
   - 提供技术变更和依赖变更说明

2. **文档格式**
   - 使用清晰的标题层级
   - 分类组织更新内容
   - 提供版本信息和发布日期

3. **文档位置**
   - 命名为 `更新内容说明.md`
   - 放置在项目根目录
   - 包含在每次更新包中

4. **文档更新**
   - 每次版本更新时必须更新此文档
   - 确保文档内容与实际更新内容一致
   - 提供详细的技术变更信息

## 11. 最佳实践

### 11.1 开发者端

1. **测试更新包**
   - 在测试环境先验证更新包
   - 确保所有功能正常

2. **文档更新**
   - 同步更新相关文档
   - 提供详细的更新说明

3. **版本标记**
   - 使用 Git 标签标记版本
   - 便于追溯和管理

### 11.2 用户端

1. **备份重要数据**
   - 虽然脚本会自动备份，但建议额外备份重要数据

2. **选择合适时间**
   - 在非工作时间进行更新
   - 避免影响正常使用

3. **验证更新**
   - 更新后全面测试功能
   - 确保系统正常运行

4. **保留备份**
   - 保留更新备份一段时间
   - 确认无问题后再删除

## 12. 总结

本更新分发方案提供了一套完整的系统更新机制，包括：

1. **自动化打包**：一键创建全量更新包
2. **安全更新**：自动备份、环境检查、错误处理
3. **便捷回滚**：支持多版本备份和快速回滚
4. **灵活分发**：支持多种分发方式
5. **完整文档**：提供详细的使用说明和故障处理指南

通过本方案，开发者可以轻松地将系统更新分发给用户，用户也可以安全、便捷地进行系统更新和回滚。
